HTML mockups generated by Claude Code land here.

One file per screen, named like `S04_dashboard.html`. After generating multiple screens, there should also be an `index.html` at the root of this folder linking to all of them as a gallery.

This file can be deleted once there are real mockups here.
